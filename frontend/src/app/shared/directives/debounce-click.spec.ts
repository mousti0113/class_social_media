import { DebounceClick } from './debounce-click';

describe('DebounceClick', () => {
  it('should create an instance', () => {
    const directive = new DebounceClick();
    expect(directive).toBeTruthy();
  });
});
